<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Theme chính của app -->
    <style name="Theme.AccountApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Màu chính cho app -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orangeMid</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/orangeMid</item>

        <!-- Các màu hệ thống khác nếu cần -->
        <item name="colorAccent">@color/orange</item>
    </style>

</resources>
